services:
  mmsegmentation: 
    build:
      context: .
      dockerfile: ../docker/mmsegmentation/Dockerfile
    ipc: host
    stdin_open: true
    tty: true
    volumes:
      - ../models:/models
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]